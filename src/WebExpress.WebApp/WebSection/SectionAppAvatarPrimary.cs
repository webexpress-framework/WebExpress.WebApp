using WebExpress.WebCore.WebSection;

namespace WebExpress.WebApp.WebSection
{
    /// <summary>
    /// Names the 'app.avatar.primary' slot of the WebApp page layout: a placeholder section into which fragments can be inserted to place content at that position.
    /// </summary>
    public class SectionAppAvatarPrimary : ISection
    {
    }
}
